bool test(String in, int location, String what, int whereline = 0, int whereFistChar = 0, String supp = String("") ){
	// file true location
	if (whereline != 0)
		// get to line of the file to test
		// if (whereFistChar) test anywhere in the line
		testLine(line, in, whereFistChar);
		else
		testLine(line, in, whereFistChar);
}
bool testLine(String line, String in, int whereFistChar = 0){
	if ( whereFistChar == 0){
		// search enaywhere in line
	} else {
		// get to the position and search strictly there
	}
}
int main (){
	String mainfile = $1;
	... $2 ..
	bool condition = true; 
	condition = condition && test("fid", 2,)
}